using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardSetInfoTextHeight : MonoBehaviour {
/* Old code, don't use
    private float newHeight = 0;
	// Use this for initialization
	void Start () {
        
        foreach (Transform child in transform)
        {
            newHeight += child.GetComponent<RectTransform>().rect.height;
        }

        RectTransform rect = this.GetComponent<RectTransform>();
        float rectWidth = rect.rect.width;

        rect.sizeDelta = new Vector2(rectWidth, newHeight);
        
    }
*/
}
